[build]
  command = "node scripts/generate-sitemap.js && npm run build"
  publish = "dist"

[build.environment]
  NETLIFY = "true"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Prerender pages for crawlers
[[plugins]]
  package = "@netlify/plugin-lighthouse"